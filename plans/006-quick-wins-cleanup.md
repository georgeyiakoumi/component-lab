# Plan 006: Quick wins — dead code removal, wrapper migration, npm audit fix

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise.
>
> **Drift check (run first)**: `git diff --stat bbac9f4..HEAD -- lib/component-tree-v2.poc.ts components/playground/edit-panel-section.tsx components/playground/sections/child-placement-section.tsx lib/supabase/ .env.example package.json`

## Status

- **Priority**: P2
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: tech-debt
- **Planned at**: commit `bbac9f4`, 2026-08-26

## Why this matters

Four small cleanups that reduce cognitive overhead and dead code:

1. **Dead POC file**: `lib/component-tree-v2.poc.ts` (239 lines) has zero imports — it was a compile-time schema test that served its purpose.
2. **EditPanelSection wrapper**: The backward-compat wrapper has only 1 remaining usage. Migrating it completes the compound-pattern refactor.
3. **Unused Supabase scaffolding**: `lib/supabase/client.ts` and `lib/supabase/server.ts` are never imported. The Supabase packages add weight to `node_modules` and confuse onboarding.
4. **npm audit vulnerabilities**: 9 high-severity vulns in dev deps (vite, ws) — fixable with `npm audit fix`.

## Current state

### POC file
`lib/component-tree-v2.poc.ts` — 239 lines. Docstring: "This file exists as a compile-time test of the v2 schema." Zero imports from any other file.

### EditPanelSection wrapper
`components/playground/edit-panel-section.tsx:134-171` — backward-compat `EditPanelSection` function that wraps `EditSection`. Only one usage:

`components/playground/sections/child-placement-section.tsx:20,42`:
```tsx
import { EditPanelSection } from "@/components/playground/edit-panel-section"
// ...
    <EditPanelSection
      icon={AlignCenterVertical}
      title={`Child — ${parentEffectiveDisplay}`}
      defaultOpen
      hasValues={sectionHasValues("childPlacement")}
      onClear={() => clearSection("childPlacement")}
    >
```

The compound pattern equivalent is `EditSection` (same file, already exported).

### Supabase scaffolding
- `lib/supabase/client.ts` — creates browser Supabase client. Zero imports.
- `lib/supabase/server.ts` — creates server Supabase client. Zero imports.
- `package.json` deps: `@supabase/ssr`, `@supabase/supabase-js`
- `.env.example` declares `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### npm audit
9 high-severity vulnerabilities in transitive dev deps (vite 8.0.0-8.0.15, ws 8.0.0-8.20.1).

## Commands you will need

| Purpose   | Command              | Expected on success |
|-----------|----------------------|---------------------|
| Typecheck | `npx tsc --noEmit`   | exit 0              |
| Tests     | `npm test`           | all pass            |
| Build     | `npm run build`      | exit 0              |
| Audit     | `npm audit`          | 0 high severity     |

## Scope

**In scope**:
- `lib/component-tree-v2.poc.ts` (delete)
- `components/playground/edit-panel-section.tsx` (remove wrapper function and its export)
- `components/playground/sections/child-placement-section.tsx` (update import and usage)
- `lib/supabase/client.ts` (delete)
- `lib/supabase/server.ts` (delete)
- `package.json` (remove Supabase deps, run npm audit fix)
- `package-lock.json` (regenerated by npm)
- `.env.example` (remove Supabase vars)
- `netlify.toml` (remove Supabase env var comments)

**Out of scope**:
- Any `components/ui/` files
- Any playground logic
- CLAUDE.md (Supabase is listed as "planned for auth" — leave the mention, the deps just aren't needed yet)

## Git workflow

- Branch: `advisor/006-quick-wins-cleanup`
- One commit per logical unit (POC deletion, wrapper migration, Supabase removal, audit fix).
- Commit message style: `chore: <description>`
- Do NOT push or open a PR.

## Steps

### Step 1: Delete the POC file

Delete `lib/component-tree-v2.poc.ts`.

**Verify**: `test ! -f lib/component-tree-v2.poc.ts && echo "deleted"` → "deleted"
**Verify**: `npx tsc --noEmit` → exit 0

### Step 2: Migrate the last EditPanelSection usage

In `components/playground/sections/child-placement-section.tsx`:
1. Change the import from `EditPanelSection` to `EditSection`:
   ```tsx
   import { EditSection } from "@/components/playground/edit-panel-section"
   ```
2. Change the JSX from `<EditPanelSection ...>` to `<EditSection ...>`:
   ```tsx
   <EditSection
     icon={AlignCenterVertical}
     title={`Child — ${parentEffectiveDisplay}`}
     defaultOpen
     hasValues={sectionHasValues("childPlacement")}
     onClear={() => clearSection("childPlacement")}
   >
   ```
   (The props are identical — `EditPanelSection` was a pass-through wrapper.)

**Verify**: `npx tsc --noEmit` → exit 0

### Step 3: Remove the EditPanelSection wrapper

In `components/playground/edit-panel-section.tsx`:
1. Delete the `EditPanelSectionProps` interface (lines 137-144)
2. Delete the `EditPanelSection` function (lines 146-159)
3. Delete the TODO comment (line 135)
4. Remove `EditPanelSection` from the export list (line 170)
5. Remove `EditPanelSectionProps` from the type export (line 172)

**Verify**: `grep -n 'EditPanelSection' components/playground/edit-panel-section.tsx` → no matches
**Verify**: `npx tsc --noEmit` → exit 0

### Step 4: Remove Supabase scaffolding

1. Delete `lib/supabase/client.ts`
2. Delete `lib/supabase/server.ts`
3. Remove `@supabase/ssr` and `@supabase/supabase-js` from `package.json` dependencies
4. In `.env.example`, remove the lines:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   # SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   ```
   and the `# ── Supabase ──` section header
5. In `netlify.toml`, remove the comments referencing Supabase env vars (lines 14-15):
   ```
   #   NEXT_PUBLIC_SUPABASE_URL
   #   NEXT_PUBLIC_SUPABASE_ANON_KEY
   ```
6. Run `npm install` to regenerate `package-lock.json`

**Verify**: `test ! -f lib/supabase/client.ts && echo "deleted"` → "deleted"
**Verify**: `grep -rn 'supabase' package.json` → no matches (case-insensitive search may still find it in comments — verify it's only in scripts or unrelated)
**Verify**: `npx tsc --noEmit` → exit 0

### Step 5: Fix npm audit vulnerabilities

Run `npm audit fix` to update transitive dev deps.

**Verify**: `npm audit 2>&1 | grep 'high'` → no "high" severity lines (or significantly fewer)
**Verify**: `npm test` → all pass
**Verify**: `npm run build` → exit 0

## Test plan

No new tests. Existing tests cover all remaining functionality. The deleted code was unused.

## Done criteria

- [ ] `lib/component-tree-v2.poc.ts` does not exist
- [ ] `grep -rn 'EditPanelSection' components/` returns no matches
- [ ] `lib/supabase/` directory does not exist
- [ ] `grep -i 'supabase' package.json` returns no matches in dependencies
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npm test` exits 0
- [ ] `npm run build` exits 0
- [ ] No files outside the in-scope list are modified

## STOP conditions

Stop and report back if:

- Any in-scope file has changed since `bbac9f4`.
- `grep -rn 'from.*@/lib/supabase' app/ components/ lib/` returns any matches (Supabase is actually used somewhere missed by the audit).
- `EditPanelSection` is imported anywhere other than `child-placement-section.tsx`.
- `npm audit fix` introduces a breaking change (build or tests fail after fix).

## Maintenance notes

- When Supabase auth is added in a future milestone, re-install the packages and re-create the client files. The scaffolding pattern in `.claude/project-setup.md` handles this.
- The `component-tree-v2.poc.ts` design doc URL (Notion) should be preserved in an ADR if the schema design decisions matter for future work.
